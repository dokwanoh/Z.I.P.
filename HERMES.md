# Hermes Usage

Hermes should call the same router and benchmark harness exposed by `zip_agent.cli`. Use JSON fixtures for benchmark rows, preserve Korean engineering intent, and emit compact patch-first responses with evidence logs.
